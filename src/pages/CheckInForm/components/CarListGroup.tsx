import { ListGroup } from 'react-bootstrap';
import styled from 'styled-components';

const CarListGroup = styled(ListGroup)`
  max-height: 13.8rem;
  overflow-x: hidden;
  overflow-y: scroll;
`;

export default CarListGroup;
