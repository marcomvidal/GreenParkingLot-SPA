import { InputGroup } from "react-bootstrap";
import styled from "styled-components";

const SearchIconContainer = styled(InputGroup.Text)`
  border-top-left-radius: 0;
  border-bottom-left-radius: 0;
  border-left: 0;
`;

export { SearchIconContainer };